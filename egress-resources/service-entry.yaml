apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: nid-service
  namespace: ec-eg
spec:
  hosts:
    - nid.internal.local
  location: MESH_EXTERNAL
  resolution: STATIC
  ports:
    - number: 32000
      name: http
      protocol: HTTP
  endpoints:
    - address: 192.168.1.125
      ports:
        http: 32000
